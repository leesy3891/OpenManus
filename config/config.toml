# ======================================================================================
# Profiled execution config example.
# Copy to config/config.toml and fill in real values.
# ======================================================================================

# Main orchestration LLM (API-based, used by PlanningFlow.llm via config_name="default")
[llm]
api_type = "openai"
model = "gpt-4o"
base_url = "https://api.openai.com/v1"
api_key = "sk-..."
max_tokens = 4096
temperature = 0.0

# Local HuggingFace profiled sub-LLM (used by ProfiledExecutorAgent via config_name="profiled_qwen")
[llm.profiled_qwen]
api_type = "hf_local"
model = "Qwen/Qwen3-32B"
base_url = ""
api_key = ""
max_tokens = 2048
temperature = 0.0
device_map = "auto"
torch_dtype = "bfloat16"
load_in_4bit = false
load_in_8bit = false
attn_implementation = "eager"
enable_thinking = true
profile_cache = true
profile_dir = "record"

# Optional configuration for specific LLM models
#[llm.vision]
#model = "claude-3-7-sonnet"    # The vision model to use
#base_url = "https://api.openai.com/v1"    # API endpoint URL for vision model
#api_key = "sk-..."    # Your API key for vision model
#max_tokens = 8192     # Maximum number of tokens in the response
#temperature = 0.0     # Controls randomness for vision model

# [llm.vision] #OLLAMA VISION:
# api_type = 'ollama'
# model = "llama3.2-vision"
# base_url = "http://localhost:11434/v1"
# api_key = "ollama"
# max_tokens = 4096
# temperature = 0.0

# Optional configuration for specific browser configuration
# [browser]
# Whether to run browser in headless mode (default: false)
#headless = false
# Disable browser security features (default: true)
#disable_security = true
# Extra arguments to pass to the browser
#extra_chromium_args = []
# Path to a Chrome instance to use to connect to your normal browser
# e.g. '/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'
#chrome_instance_path = ""
# Connect to a browser instance via WebSocket
#wss_url = ""
# Connect to a browser instance via CDP
#cdp_url = ""

# Optional configuration, Proxy settings for the browser
# [browser.proxy]
# server = "http://proxy-server:port"
# username = "proxy-username"
# password = "proxy-password"

# Optional configuration, Search settings.
# [search]
# Search engine for agent to use. Default is "Google", can be set to "Baidu" or "DuckDuckGo".
#engine = "Google"
